# Dotfiles symlinked on my machine

These are the ~/.config files from my machine.

### Install with mise:
```bash
curl https://mise.run | sh
git clone https://github.com/hdweiss/dotfiles.git ~/.dotfiles
mise bootstrap dotfiles apply
```
